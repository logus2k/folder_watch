# folder_watch — runtime dependencies (Python 3.12)
# Pinned for reproducibility between host venv and container. Mirrors agent_scheduler.

fastapi==0.121.1         # admin API
uvicorn==0.49.0          # ASGI server (matches agent_bus)
valkey-glide==2.4.2      # bus event emission — pinned to match agent_bus wire client
pydantic==2.13.4         # models + vendored EventEnvelope (match agent_bus)
python-dotenv==1.2.2     # .env loading for local/dev

# dev/test
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1            # FastAPI TestClient
